version: "3.4"

services:
  web:
    build:
      context: .
      target: builder
    restart: unless-stopped
    ports:
      - 80:3000
    environment:
      API_URL: https://covid19sayac.xyz
      NUXT_HOST: 0.0.0.0
      NUXT_PORT: 3000
  # web:
  #   image: nginx:alpine
  #   restart: unless-stopped
  #   ports:
  #     - 80:80
  #   volumes:
  #     - ./dist:/usr/share/nginx/html
